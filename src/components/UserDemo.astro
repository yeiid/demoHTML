---
// Simplified User Management Demo - Visual showcase without complex interactivity
---

<section id="user-demo" class="container">
    <div class="text-center mb-16">
        <h2 class="text-5xl font-bold mb-4">
            Gestión de <span class="gradient-text">Usuarios</span>
        </h2>
        <p class="text-muted max-w-2xl mx-auto">
            Registra miembros, gestiona membresías y controla accesos en
            segundos.
        </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <!-- Features List -->
        <div class="glass p-8">
            <h3 class="text-2xl font-semibold mb-6">Funcionalidades Clave</h3>
            <ul class="space-y-4">
                <li class="feature-item">
                    <span class="feature-icon">👤</span>
                    <div>
                        <h4 class="font-semibold">Registro Rápido</h4>
                        <p class="text-sm text-muted">
                            Formulario simplificado con campos esenciales
                        </p>
                    </div>
                </li>
                <li class="feature-item">
                    <span class="feature-icon">💳</span>
                    <div>
                        <h4 class="font-semibold">Planes Flexibles</h4>
                        <p class="text-sm text-muted">
                            Mensual, trimestral, anual con precios
                            personalizados
                        </p>
                    </div>
                </li>
                <li class="feature-item">
                    <span class="feature-icon">📅</span>
                    <div>
                        <h4 class="font-semibold">Control de Asistencia</h4>
                        <p class="text-sm text-muted">
                            Registro automático de check-in/check-out
                        </p>
                    </div>
                </li>
                <li class="feature-item">
                    <span class="feature-icon">🔔</span>
                    <div>
                        <h4 class="font-semibold">Alertas de Vencimiento</h4>
                        <p class="text-sm text-muted">
                            Notificaciones automáticas de renovación
                        </p>
                    </div>
                </li>
                <li class="feature-item">
                    <span class="feature-icon">📊</span>
                    <div>
                        <h4 class="font-semibold">Historial Completo</h4>
                        <p class="text-sm text-muted">
                            Pagos, asistencias y progreso del miembro
                        </p>
                    </div>
                </li>
            </ul>
        </div>

        <!-- Member Profile Preview -->
        <div class="glass p-8 bg-gradient-to-br from-primary/10 to-transparent">
            <div class="flex items-center gap-4 mb-6">
                <div class="avatar">JP</div>
                <div>
                    <h4 class="text-xl font-bold">Juan Pérez</h4>
                    <p class="text-muted text-sm">Plan Mensual - $30</p>
                </div>
                <span class="badge ml-auto">Activo</span>
            </div>

            <div class="grid grid-cols-3 gap-4 mb-6">
                <div class="stat-card glass">
                    <small class="text-muted">Asistencias</small>
                    <div class="text-2xl font-bold">24</div>
                </div>
                <div class="stat-card glass">
                    <small class="text-muted">Días Activo</small>
                    <div class="text-2xl font-bold">30</div>
                </div>
                <div class="stat-card glass">
                    <small class="text-muted">Próximo Pago</small>
                    <div class="text-lg font-bold">10 Mar</div>
                </div>
            </div>

            <div class="space-y-3">
                <div class="info-row">
                    <span class="text-muted">Email:</span>
                    <span>juan.perez@email.com</span>
                </div>
                <div class="info-row">
                    <span class="text-muted">Teléfono:</span>
                    <span>+1 (555) 123-4567</span>
                </div>
                <div class="info-row">
                    <span class="text-muted">Fecha de Registro:</span>
                    <span>10 Feb 2026</span>
                </div>
                <div class="info-row">
                    <span class="text-muted">Método de Pago:</span>
                    <span>Tarjeta •••• 4242</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .feature-item {
        display: flex;
        align-items: flex-start;
        gap: 1rem;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.03);
        border-radius: 0.5rem;
        transition: all 0.3s ease;
    }

    .feature-item:hover {
        background: rgba(255, 255, 255, 0.05);
        transform: translateX(5px);
    }

    .feature-icon {
        font-size: 2rem;
        flex-shrink: 0;
    }

    .avatar {
        width: 64px;
        height: 64px;
        background: var(--primary);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        font-size: 1.5rem;
        font-weight: 700;
    }

    .badge {
        background: var(--secondary);
        color: var(--bg);
        padding: 0.25rem 1rem;
        border-radius: 2rem;
        font-size: 0.8rem;
        font-weight: 700;
    }

    .stat-card {
        padding: 1rem;
        text-align: center;
    }

    .info-row {
        display: flex;
        justify-content: space-between;
        padding: 0.75rem 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }

    .info-row:last-child {
        border-bottom: none;
    }

    .mb-16 {
        margin-bottom: 4rem;
    }

    @media (max-width: 768px) {
        .grid-cols-3 {
            grid-template-columns: 1fr;
        }
    }
</style>
